schema_version: 1

device:
  id: "device_0Q0MPJ4Z2ENZ97XWETRESKZGTH"
  name: "example-ubuntu-desktop"
  os: "linux"
  architecture: "x86_64"
  class:
    profile: "desktop"
    gui: "enabled"
    docker_mode: "none"
    execution_policy: "source-lsp-only"

workspace_roots:
  personal: "${HOME}/Developer/personal"
  organization: "${HOME}/Developer/organization"

materialization:
  default_mode: "absent"
  include:
    - selector: "portfolio:active-projects"
      workspace_root: "personal"
      mode: "active"

harnesses:
  - "codex"
  - "claude-code"

state:
  path: "${XDG_STATE_HOME}/github-device-sync"

# GENERATED FILE - DO NOT EDIT DIRECTLY
# generator: gds
# bundle: 0.4.0-dev
# source-tree-digest: sha256:13bf1662138ccfbbf98c94bd252958f83c7d76e3040825245b9dbab87c219e5c
# input-digest: sha256:854f63444692b2b2354abcab35a625754124c77dc6ad3cd2909304284724d83c
# output-digest: sha256:91855b4f5ca00df3f237b7e8aeadf27b680e86e890d9ee49b2683f5c96eca68f
# edit-source:
#   - .gds/repository.yaml
#   - policies/base/repository-default.yaml
#   - policies/repositories/github-device-sync.yaml
#   - policies/roles/public-module.yaml
#   - templates/agents/repository.md.tmpl
#   - templates/github-actions/go.yml.tmpl
#   - templates/harnesses/claude.md.tmpl
name: gds-ci

on:
  pull_request:
    branches:
      - "main"
  push:
    branches:
      - "main"

permissions: {}

concurrency:
  group: gds-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fast:
    name: GDS fast
    permissions:
      contents: read
    uses: NDDev-OpenNetwork/ci-workflows/.github/workflows/go-ci.yml@da43707a70644e98a1fb5f79e46ac643b4829550
    with:
      fetch_depth: 0
      # Public hosted CI uses GitHub's service cache; the pinned reusable
      # workflow owns cache keys and invalidation, so no runner-local state is trusted.
      cache: true

      runner: "ubuntu-latest"
      go_version: "1.26.5"
      test_command: "go test ./..."
      timeout_minutes: 30
  pr-required:
    name: GDS PR required
    # Serialized behind fast: both tiers run the integrated assurance scenario,
    # which asserts wall-clock budgets, and running two of them side by side on
    # one host measures contention rather than the code. It is also the right
    # gate order — there is no value in the expensive tier once fast is red.
    needs: fast
    permissions:
      contents: read
    uses: NDDev-OpenNetwork/ci-workflows/.github/workflows/go-ci.yml@da43707a70644e98a1fb5f79e46ac643b4829550
    with:
      fetch_depth: 0
      cache: true
      runner: "ubuntu-latest"
      go_version: "1.26.5"
      build_command: "go build -trimpath ./core/cmd/gds"
      test_command: "go test ./... && python3 -m pip install --quiet --require-hashes -r requirements/test.txt && python3 -m pytest"
      timeout_minutes: 30

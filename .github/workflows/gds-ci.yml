# GENERATED FILE - DO NOT EDIT DIRECTLY
# generator: gds
# bundle: 0.4.0-dev
# source-tree-digest: sha256:6c532c0e1a28628dc42e3f000d6d4a31dcfd02242049186235cb818af736b305
# input-digest: sha256:f8d27a757c33b1411bc90278c3d81fb8eceb6fea98be49900307d0804fa21e73
# output-digest: sha256:d8b671102ab8e23cad2d3acf529f9bc8f52eade072498e47f9b4752671bc1c71
# edit-source:
#   - .gds/repository.yaml
#   - policies/base/repository-default.yaml
#   - policies/repositories/github-device-sync.yaml
#   - policies/roles/control-plane.yaml
#   - templates/agents/repository.md.tmpl
#   - templates/github-actions/go.yml.tmpl
#   - templates/harnesses/claude.md.tmpl
name: gds-ci

on:
  pull_request:
    branches:
      - "main"
  push:
    branches:
      - "main"

permissions: {}

concurrency:
  group: gds-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fast:
    name: GDS fast
    permissions:
      contents: read
    uses: NDDev-OpenNetwork/ci-workflows/.github/workflows/go-ci.yml@da43707a70644e98a1fb5f79e46ac643b4829550
    with:
      fetch_depth: 0
      # The fleet creates one virtual machine per job and destroys it after,
      # so there is no warm cache for setup-go to own and nothing for a stale
      # one to corrupt. Delegating cache ownership would instead add a
      # dependency on the GitHub cache service, which the fleet contract does
      # not promise and which this repository does not need: the golden image
      # bakes the exact Go toolchain named below.
      cache: false
      runner: "ubuntu-latest"
      go_version: "1.26.5"
      test_command: "go test ./..."
      timeout_minutes: 30
  pr-required:
    name: GDS PR required
    # Serialized behind fast: both tiers run the integrated assurance scenario,
    # which asserts wall-clock budgets, and running two of them side by side on
    # one host measures contention rather than the code. It is also the right
    # gate order — there is no value in the expensive tier once fast is red.
    needs: fast
    permissions:
      contents: read
    uses: NDDev-OpenNetwork/ci-workflows/.github/workflows/go-ci.yml@da43707a70644e98a1fb5f79e46ac643b4829550
    with:
      fetch_depth: 0
      cache: false
      runner: "ubuntu-latest"
      go_version: "1.26.5"
      build_command: "go build -trimpath ./core/cmd/gds"
      test_command: "go test ./... && python3 -m pytest"
      timeout_minutes: 30

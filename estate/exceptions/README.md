# Estate exceptions

Sparse, explicit overrides for repositories a selector classifies wrongly.

An exception is a last resort: it moves one repository out of the rule that
would otherwise cover it, which means the rule no longer describes the estate.
Prefer narrowing the selector. When an exception is genuinely right, say why in
the file -- the reason is the part that decays, not the syntax.

schema_version: 1

installation:
  id: "installation:github-organization"
  provider: "github"
  account_type: "organization"
  account_login: "example-org"
  app_installation_id_source: "secure-runtime"

management:
  discover_all_repositories: true
  default_mode: "managed"

permissions:
  repository_selection: "all"
  repository:
    actions: "read"
    administration: "read"
    checks: "read"
    contents: "read"
    metadata: "read"
    pull_requests: "read"
  organization: {}

credentials:
  strategy: "github-app-installation-token"
  secret_ref: "secret:gds/github-app/organization"
